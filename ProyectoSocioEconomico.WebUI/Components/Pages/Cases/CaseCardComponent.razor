@namespace ProyectoSocioEconomico.WebUI.Components.Pages
@*
  CaseCardComponent.razor
  Propósito: Componente que muestra la información resumida de un caso (campaña) con
  progreso y botón para ver detalles. Usado en listados de casos.
*@
@using ProyectoSocioEconomico.Domain.Entities
@using System.Globalization

<article class="cap-card">
    <div class="cap-card__image-wrap">
        @if (!string.IsNullOrEmpty(ImageUrl))
        {
            <img class="cap-card__image" src="@ImageUrl" alt="@Title" />
        }
        else
        {
            <img class="cap-card__image" src="https://bulma.io/assets/images/placeholders/1280x960.png" alt="Placeholder" />
        }
        
        <span class="cap-card__tag cap-card__tag--@Category?.ToLower().Replace(" ", "-")">
            @Category?.ToUpper()
        </span>
    </div>

    <div class="cap-card__body">
        <h2 class="cap-card__title">@Title</h2>
        <p class="cap-card__desc">@Description</p>

        <div class="cap-card__progress-block">
            <div class="cap-card__progress-header">
                <span class="cap-card__progress-label">Recaudado: @MoneyRaised</span>
                <span class="cap-card__progress-pct">@Progress%</span>
            </div>
            <progress class="bulma-progress bulma-is-success cap-card__progress-bar"
                      value="@Progress" max="100">
                @Progress%
            </progress>
        </div>

        <a href="/cases/@Id" class="bulma-button bulma-is-warning bulma-is-fullwidth cap-card__btn">
            <strong>Ver Detalles del Caso</strong>
        </a>
    </div>
</article>

@code {
    [Parameter] public int Id { get; set; }
    [Parameter] public string Title { get; set; } = string.Empty;
    [Parameter] public string Description { get; set; } = string.Empty;
    [Parameter] public string Category { get; set; } = string.Empty;
    [Parameter] public string MoneyRaised { get; set; } = string.Empty;
    [Parameter] public int Progress { get; set; }
    [Parameter] public string? ImageUrl { get; set; }
}
